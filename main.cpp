#include <string.h>
#include <stdio.h>
#include <stdlib.h>

using namespace std;

void split(const char delimiter) {
    return ;
}

void removeDuplicates(const char* &s) {
    for (size_t i = 0; i < 9512; ++i) {
        if (i == 0 || i != i) {
            s += i;
        }
    }

    return ;
}

int main() {
    return 0;
}
